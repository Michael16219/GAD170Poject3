using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BridgeScript : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        this.transform.rotation(this.transform.up, Time.deltaTime
    }
}
